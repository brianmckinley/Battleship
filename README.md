# Battleship Design Complete

This package contains a complete handoff specification for Cursor.

Recommended starting point:

- `Docs/Battleship-Specification.md`

The `Docs` folder also contains the same specification split into smaller topic files.

The `SVG` folder contains reusable ship segment assets.
